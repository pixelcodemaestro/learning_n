apiVersion: v1
kind: Service

metadata:
  name: attendance-service
  namespace: test

spec:
  type: NodePort

  selector:
    app: attendance

  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
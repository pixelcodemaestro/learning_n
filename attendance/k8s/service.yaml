apiVersion: v1
kind: Service

metadata:
  name: iphonev2-service
  namespace: marathi

spec:
  type: NodePort

  selector:
    app: iphonev2

  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30082
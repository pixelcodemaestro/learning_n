apiVersion: v1
kind: Service

metadata:
  name: attendance-service
  namespace: attendance

spec:
  type: NodePort

  selector:
    app: attendance

  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30080
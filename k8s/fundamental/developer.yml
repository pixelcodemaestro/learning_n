apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: developer-role
  namespace: rowdy

rules:
- apiGroups: ["", "apps"]

  resources:
  - pods
  - deployments
  - services
  - configmaps

  
  verbs:
  - get
  - list

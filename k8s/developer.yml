apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: developer-role
  namespace: application1

rules:
- apiGroups: ["", "apps"]

  resources:
  - pods
  - deployments
  - services

  verbs:
  - get
  - list
